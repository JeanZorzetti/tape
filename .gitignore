node_modules/
dist/
.astro/
.env
.env.*
!.env.example
*.log
.DS_Store
Thumbs.db

# Fotos originais (74 MB) e o repo de referência de rules: ficam locais,
# o build só usa src/assets. Receitas de recorte estão no handoff.md.
imagens/
awesome-cursorrules/

# Screenshots do Playwright MCP
.playwright-mcp/
/*.png

# Lista bruta de prospecção (PII de terceiros: nome/telefone/email/CNPJ de ~503 empresas).
# Fica local — é insumo operacional do script de import, não fonte versionada.
docs/leads-bruto.csv
